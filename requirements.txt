# 核心依赖
pygame>=2.5.0
# 注意：项目使用 esper 2.x 的 World/Processor API，不兼容 esper 3.x
esper>=2.5,<3.0
PyYAML>=6.0
numpy>=1.24.0

# 开发依赖
pytest>=7.0
pytest-cov>=4.0
mypy>=1.0
black>=23.0

# 可选依赖
loguru>=0.7.0
Cython>=3.0
