# 游戏设计规范文档（单机Roguelike版）

## 1. 概述

### 1.1 文档目的
本文档用于规范单机Roguelike游戏开发过程中的设计标准，确保团队成员对游戏设计有统一的理解和执行标准。涵盖游戏策划、美术设计、程序开发等各个环节的规范要求。

### 1.2 适用范围
- 游戏策划：玩法设计、数值平衡、系统设计
- 美术设计：角色、场景、UI、特效
- 程序开发：功能实现、性能优化、架构设计
- 产品经理：需求定义、版本规划

### 1.3 文档维护
- 本文档由策划团队负责维护
- 任何修改需经过评审会议讨论
- 版本更新需同步通知所有相关人员

---

## 2. 游戏核心设计

### 2.1 核心玩法

#### 2.1.1 基础信息
| 项目 | 说明 | 填写示例 |
|------|------|---------|
| 游戏名称 | 正式名称及副标题 | 《深渊回响：无尽轮回》 |
| 游戏类型 | 主要类型+次要类型 | Roguelike + ARPG |
| 核心机制 | 最核心的1-3个玩法机制 | 随机地图生成、永久死亡机制、遗物Build构筑 |
| 目标用户 | 年龄段、性别、偏好 | 18-35岁，核心向玩家，喜欢挑战和重复可玩性 |
| 游戏时长 | 单局/单次游戏预计时长 | 单局30-60分钟 |
| 游戏节奏 | 整体节奏描述 | 快节奏战斗+策略性Build选择 |

#### 2.1.2 核心体验
- **情感目标**：玩家应该感受到挑战的刺激、Build成型的成就感、面对死亡的紧张感
- **核心乐趣**：每次游戏都是全新体验，通过遗物组合创造独特Build，挑战更高难度
- **差异化**：独特的遗物联动系统、丰富的Build流派、高自由度的角色成长路径

### 2.2 游戏循环

#### 2.2.1 核心循环图（单局内）
```
[开始游戏]
    ↓
[生成随机地图]
    ↓
[选择路径：战斗/商店/事件/休息]
    ↓
[遭遇战斗/事件]
    ↓
[获得奖励：金币/遗物/卡牌/道具]
    ↓
[强化Build]
    ↓
[到达下一层] → [循环直到BOSS层]
    ↓
[击败最终BOSS 或 死亡]
    ↓
[结算本局成就/解锁内容]
    ↓
[返回主菜单 或 重新开始]
```

#### 2.2.2 元进度循环（跨局）
```
[完成一局游戏]
    ↓
[获得元货币：灵魂碎片]
    ↓
[解锁新角色/新遗物/新卡牌]
    ↓
[提升全局属性加成]
    ↓
[解锁更高难度等级]
    ↓
[开始新一局，体验新内容]
```

#### 2.2.3 长期目标
- **短期目标**（1-3局）：熟悉基础操作，完成第一次通关
- **中期目标**（10-20局）：解锁全部角色，体验不同Build流派
- **长期目标**（50+局）：通关最高难度，收集全部遗物，完成全成就

---

## 3. 系统设计规范

### 3.1 数值设计

#### 3.1.1 数值框架
| 数值类型 | 说明 | 设计原则 |
|---------|------|---------|
| 基础属性 | 生命、攻击、防御、速度 | 单局内通过遗物和升级成长 |
| 战斗属性 | 暴击、闪避、元素伤害 | 遗物提供，Build核心 |
| 特殊机制 | 连击、格挡、吸血 | 流派差异化设计 |
| 资源数值 | 金币、灵魂碎片（元货币） | 金币单局消耗，灵魂碎片跨局积累 |

#### 3.1.2 单局数值范围规范
```
初始生命值：50 - 80（根据角色不同）
生命值上限：200 - 500（单局成长后）
初始攻击力：10 - 15
攻击力上限：50 - 150（遗物加成后）

百分比属性上限：
- 暴击率：最高75%
- 闪避率：最高40%
- 伤害加成：无上限，但收益递减
- 冷却缩减：最高50%
```

#### 3.1.3 成长曲线类型
| 曲线类型 | 适用场景 | 公式示例 |
|---------|---------|---------|
| 线性 | 基础属性升级 | y = kx + b |
| 指数 | 遗物叠加效果 | y = a * x^b |
| 对数 | 边际递减属性 | y = a * ln(x) + b |
| S型 | 难度曲线 | 慢-快-慢 |

#### 3.1.4 数值平衡检查清单
- [ ] 不同Build流派有各自优势场景
- [ ] 没有绝对强势的遗物组合
- [ ] 高风险Build有高回报潜力
- [ ] 新手Build易于上手但上限不低
- [ ] 数值膨胀在单局内可控
- [ ] 难度曲线平滑，后期有挑战性

### 3.2 经济系统

#### 3.2.1 货币体系
| 货币名称 | 类型 | 获取途径 | 主要用途 | 产出控制 | 存储上限 |
|---------|------|---------|---------|---------|---------|
| 金币 | 单局货币 | 战斗掉落、事件奖励 | 商店购买、升级 | 中等 | 单局9999 |
| 灵魂碎片 | 元货币 | 通关奖励、成就解锁 | 解锁新内容 | 长期积累 | 无上限 |
| 精华 | 稀有货币 | BOSS掉落、隐藏房间 | 高级商店、复活 | 稀缺 | 单局99 |

#### 3.2.2 经济循环设计
```
单局内：
玩家投入时间/技巧
        ↓
    战斗胜利 → 获得金币/遗物
        ↓
    消耗金币 → 购买/升级
        ↓
    挑战更强敌人
        ↓
    [循环直到结束]

跨局：
单局表现 → 灵魂碎片
        ↓
    解锁新内容
        ↓
    提升后续游戏体验
        ↓
    [循环]
```

#### 3.2.3 商店设计
| 商店类型 | 出现位置 | 商品内容 | 刷新机制 |
|---------|---------|---------|---------|
| 普通商店 | 每层随机位置 | 遗物、消耗品、卡牌 | 每层刷新一次 |
| 黑市 | 隐藏房间 | 稀有遗物、诅咒物品 | 固定商品 |
| BOSS商店 | BOSS战后 | 高级遗物、角色升级 | 通关后刷新 |

### 3.3 进度系统

#### 3.3.1 元进度系统（跨局解锁）
| 解锁类型 | 解锁条件 | 解锁内容 |
|-------|---------|---------|
| 角色解锁 | 完成指定成就 | 新可玩角色 |
| 遗物解锁 | 灵魂碎片购买 | 新遗物加入随机池 |
| 难度解锁 | 通关当前难度 | 更高难度等级 |
| 剧情解锁 | 多次通关 | 背景故事碎片 |

#### 3.3.2 难度等级设计
| 难度 | 名称 | 敌人强化 | 特殊规则 |
|-----|------|---------|---------|
| 0 | 新手 | 无 | 死亡可复活1次 |
| 1 | 普通 | 生命+20% | 标准规则 |
| 2 | 困难 | 生命+50%，攻击+20% | 精英怪更频繁 |
| 3 | 噩梦 | 生命+100%，攻击+50% | BOSS有额外技能 |
| 4 | 地狱 | 生命+200%，攻击+100% | 每关时间限制 |

#### 3.3.3 成就系统
| 成就类型 | 示例 | 奖励设计 |
|---------|------|---------|
| 通关成就 | 首次通关/难度通关 | 灵魂碎片+解锁内容 |
| 挑战成就 | 无伤通关/速通 | 稀有外观+大量灵魂碎片 |
| Build成就 | 使用特定流派通关 | 该流派相关解锁 |
| 探索成就 | 发现隐藏内容 | 剧情碎片+灵魂碎片 |
| 收集成就 | 收集全部遗物 | 纪念性奖励+称号 |

---

## 4. 关卡/场景设计

### 4.1 地图生成系统

#### 4.1.1 地图结构
```
第1层：起始层
├── 入口房间
├── 2-3个普通战斗房间
├── 1个精英房间（可选）
├── 1个商店/事件房间
└── BOSS房间

第2-4层：标准层
├── 入口
├── 3-5个普通战斗房间
├── 1-2个精英房间
├── 1-2个商店/事件/休息房间
├── 1个隐藏房间（概率出现）
└── BOSS房间

第5层：最终层
├── 入口
├── 1个商店（最终补给）
└── 最终BOSS房间
```

#### 4.1.2 房间类型
| 房间类型 | 说明 | 奖励 |
|---------|------|------|
| 普通战斗 | 标准敌人遭遇战 | 金币+小概率遗物 |
| 精英战斗 | 强力敌人+特殊机制 | 金币+必掉遗物 |
| BOSS战 | 章节BOSS | 大量金币+精华+高级遗物 |
| 商店 | 购买物品 | 消耗金币 |
| 事件 | 随机事件选择 | 视选择而定 |
| 休息点 | 恢复生命或升级卡牌 | 无战斗 |
| 隐藏房间 | 秘密区域 | 稀有奖励 |

#### 4.1.3 地图生成规则
- 每层地图节点数：8-12个
- 分支路径：2-4条可选路线
- 最短路径：必须经过至少3个战斗房间才能到达BOSS
- 回溯限制：不能返回已完成的房间

### 4.2 战斗房间设计

#### 4.2.1 普通战斗模板
| 项目 | 内容 |
|------|------|
| 房间ID | COM_XXX |
| 敌人类型 | 普通怪组合 |
| 敌人数量 | 3-6只 |
| 战斗时长 | 30-60秒 |
| 环境机制 | 可选（陷阱、障碍物） |

#### 4.2.2 精英战斗模板
| 项目 | 内容 |
|------|------|
| 房间ID | ELITE_XXX |
| 敌人类型 | 精英怪+普通怪 |
| 敌人数量 | 1只精英+2-4只普通 |
| 战斗时长 | 60-90秒 |
| 特殊机制 | 精英怪有独特技能 |

#### 4.2.3 BOSS战设计
| 项目 | 内容 |
|------|------|
| BOSS名称 | |
| 阶段数 | 2-3阶段 |
| 总生命值 | 根据难度调整 |
| 阶段转换 | 血量降至70%/30% |
| 阶段1 | 基础攻击模式 |
| 阶段2 | 新增技能，攻击频率提升 |
| 阶段3 | 狂暴模式，全屏攻击 |
| 击败奖励 | 大量金币+精华+选择遗物 |

### 4.3 场景设计规范

#### 4.3.1 场景主题
| 层数 | 主题 | 视觉风格 |
|-----|------|---------|
| 第1层 | 废弃村庄 | 破败、阴郁 |
| 第2层 | 幽暗森林 | 神秘、危险 |
| 第3层 | 古代遗迹 | 庄严、神秘 |
| 第4层 | 深渊边缘 | 黑暗、压抑 |
| 第5层 | 虚空核心 | 混沌、终极 |

#### 4.3.2 场景布局原则
- **战斗空间**：足够玩家移动和闪避
- **视觉引导**：明确指示敌人位置和出口
- **环境互动**：可破坏物体、可利用的陷阱
- **氛围营造**：配合主题的光影和特效

---

## 5. 角色/Build设计

### 5.1 角色设计模板

#### 5.1.1 基础信息
| 项目 | 内容 |
|------|------|
| 角色ID | CHAR_XXX |
| 角色名称 | |
| 角色定位 | 战士/刺客/法师/射手/召唤 |
| 初始生命 | 60-80 |
| 初始金币 | 100 |
| 特色机制 | 每个角色独特玩法 |
| 解锁条件 | 初始/灵魂碎片购买 |

#### 5.1.2 角色示例
| 角色 | 定位 | 特色机制 | 初始遗物 |
|------|------|---------|---------|
| 骑士 | 战士 | 格挡系统，高生存 | 生锈盾牌：格挡时减伤50% |
| 刺客 | 刺客 | 连击系统，高爆发 | 暗影之刃：连击数+1伤害 |
| 法师 | 法师 | 元素融合，AOE | 元素法典：元素伤害+20% |
| 游侠 | 射手 | 距离加成，风筝 | 鹰眼：距离越远伤害越高 |
| 死灵 | 召唤 | 召唤亡灵作战 | 亡者之书：击败敌人召唤骷髅 |

### 5.2 遗物系统

#### 5.2.1 遗物类型
| 类型 | 说明 | 示例 |
|------|------|------|
| 攻击型 | 增强输出能力 | 暴击率提升、伤害加成 |
| 防御型 | 增强生存能力 | 生命上限、减伤、回血 |
| 功能型 | 改变游戏机制 | 额外金币、商店折扣 |
| 诅咒型 | 双刃剑效果 | 高增益+负面效果 |
| 传说型 | 独特强力效果 | 改变核心玩法 |

#### 5.2.2 遗物稀有度
| 稀有度 | 颜色 | 出现概率 | 效果强度 |
|--------|------|---------|---------|
| 普通 | 白色 | 60% | 基础加成 |
| 稀有 | 蓝色 | 30% | 中等加成 |
| 史诗 | 紫色 | 9% | 强力加成 |
| 传说 | 金色 | 1% | 改变玩法 |

#### 5.2.3 遗物联动示例
```
Build：火焰法师
├── 火焰之心（史诗）：火系伤害+50%
├── 燃烧之触（稀有）：攻击附加燃烧
├── 元素共鸣（稀有）：元素伤害互相触发
└── 凤凰之羽（传说）：死亡时满血复活（一次）

Build：暴击刺客
├── 锋利匕首（普通）：暴击率+10%
├── 暗影披风（稀有）：暴击后隐身1秒
├── 血之渴望（史诗）：暴击吸血30%
└── 死亡标记（传说）：暴击秒杀血量<20%敌人
```

### 5.3 卡牌/技能系统

#### 5.3.1 卡牌类型
| 类型 | 说明 | 示例 |
|------|------|------|
| 攻击卡 | 造成伤害 | 斩击、火球、箭雨 |
| 技能卡 | 特殊效果 | 冲刺、隐身、召唤 |
| 能力卡 | 被动加成 | 生命提升、攻击提升 |
| 诅咒卡 | 负面效果 | 但可能有特殊用途 |

#### 5.3.2 卡牌升级
- 在休息点可以升级卡牌
- 升级后效果增强或费用降低
- 部分卡牌有分支升级选择

---

## 6. UI/UX 设计规范

### 6.1 界面架构

#### 6.1.1 界面层级系统
```
Layer 0: 游戏场景层
    - 战斗场景渲染
    - 特效渲染

Layer 1: 主界面层（HUD）
    - 角色状态（血条、能量）
    - 卡牌手牌
    - 遗物栏
    - 地图缩略图

Layer 2: 功能界面层
    - 遗物详情
    - 卡牌查看
    - 地图全览
    - 设置菜单

Layer 3: 弹窗层
    - 遗物选择
    - 事件选择
    - 奖励展示
    - 确认对话框

Layer 4: 提示层
    - 伤害数字
    - 状态提示
    - 引导提示

Layer 5: 暂停/菜单层
    - 暂停菜单
    - 主菜单
    - 游戏结束界面
```

### 6.2 交互设计规范

#### 6.2.1 战斗操作
| 操作 | 方式 | 说明 |
|------|------|------|
| 移动 | WASD/摇杆 | 8方向移动 |
| 闪避 | 空格/按钮 | 短暂无敌位移 |
| 使用卡牌 | 数字键/点击 | 消耗能量使用 |
| 互动 | E键/按钮 | 拾取、开门 |

#### 6.2.2 地图界面
- 显示当前层地图结构
- 已探索房间高亮显示
- 可前往房间可点击选择
- 显示当前位置和BOSS位置

### 6.3 视觉设计规范

#### 6.3.1 色彩系统
```
主色调：#8B4513（棕色/复古）
- 用于：主按钮、选中状态、重要信息

辅助色1：#DC143C（深红）
- 用于：危险、伤害、敌人、警告

辅助色2：#228B22（森林绿）
- 用于：治疗、增益、安全区域

辅助色3：#FFD700（金色）
- 用于：传说品质、奖励、成就

辅助色4：#9370DB（紫色）
- 用于：史诗品质、魔法效果

中性色：
- 背景深：#1a1a2e
- 背景浅：#16213e
- 文字主：#e94560
- 文字次：#a0a0a0
- 分割线：#0f3460
```

#### 6.3.2 字体规范
| 层级 | 字号 | 字重 | 用途 |
|------|------|------|------|
| H1 | 36px | Bold | 游戏标题 |
| H2 | 28px | Bold | 界面标题 |
| H3 | 20px | Medium | 模块标题 |
| Body | 16px | Regular | 正文内容 |
| Caption | 14px | Regular | 辅助说明 |
| Damage | 24px | Bold | 伤害数字 |

---

## 7. 单机特色系统

### 7.1 存档系统

#### 7.1.1 存档类型
| 类型 | 说明 | 保存内容 |
|------|------|---------|
| 自动存档 | 每层结束后 | 当前进度 |
| 手动存档 | 玩家主动 | 当前状态（仅限普通难度以下） |
| 元进度存档 | 实时保存 | 解锁内容、灵魂碎片 |

#### 7.1.2 存档规则
- 单局游戏内：死亡即结束，无法读档（硬核模式）
- 元进度：自动云端同步
- 支持多存档槽位

### 7.2 每日挑战

#### 7.2.1 每日挑战设计
| 项目 | 说明 |
|------|------|
| 每日种子 | 全玩家相同随机种子 |
| 特殊规则 | 每日不同限制条件 |
| 排行榜 | 全球玩家通关时间排名 |
| 奖励 | 灵魂碎片+特殊外观 |

#### 7.2.2 挑战类型示例
- 限定角色通关
- 禁用遗物通关
- 限时通关
- 单一Build通关

### 7.3 图鉴系统

#### 7.3.1 收集内容
| 类型 | 内容 | 解锁方式 |
|------|------|---------|
| 遗物图鉴 | 所有遗物信息 | 获得过一次 |
| 敌人图鉴 | 敌人数据和掉落 | 击败对应敌人 |
| 事件图鉴 | 所有随机事件 | 触发过对应事件 |
| 剧情图鉴 | 背景故事碎片 | 多次通关解锁 |

### 7.4 自定义模式

#### 7.4.1 自定义选项
| 选项 | 说明 |
|------|------|
| 种子输入 | 使用指定种子生成地图 |
| 难度调整 | 自定义敌人强度 |
| 起始遗物 | 选择初始遗物 |
| 特殊规则 | 启用各种挑战规则 |

---

## 8. 技术规范

### 8.1 性能指标

#### 8.1.1 帧率要求
| 场景 | 目标帧率 | 最低帧率 |
|------|---------|---------|
| 战斗 | 60 FPS | 45 FPS |
| 地图界面 | 60 FPS | 30 FPS |
| 菜单界面 | 60 FPS | 30 FPS |

#### 8.1.2 加载时间
| 加载类型 | 目标时间 | 最大时间 |
|---------|---------|---------|
| 启动游戏 | < 5秒 | < 10秒 |
| 开始新局 | < 3秒 | < 5秒 |
| 进入房间 | < 1秒 | < 2秒 |
| 生成地图 | < 2秒 | < 4秒 |

#### 8.1.3 内存占用
| 平台 | 目标内存 | 最大内存 |
|------|---------|---------|
| PC | < 1GB | < 2GB |
| Switch | < 800MB | < 1.2GB |

### 8.2 存档技术

#### 8.2.1 存档结构
```json
{
  "meta_progress": {
    "soul_shards": 9999,
    "unlocked_characters": ["knight", "mage"],
    "unlocked_relics": ["relic_001", "relic_002"],
    "achievements": ["first_clear", "no_hit_boss"],
    "best_runs": [...]
  },
  "current_run": {
    "seed": 123456,
    "floor": 3,
    "player": {...},
    "relics": [...],
    "map_state": {...}
  }
}
```

### 8.3 随机生成技术

#### 8.3.1 种子系统
- 使用确定性随机数生成器
- 相同种子生成相同地图
- 支持种子分享功能

#### 8.3.2 生成算法
- 地图：基于图论的节点生成
- 遗物：加权随机池
- 敌人：根据进度和难度动态调整

---

## 9. 版本记录

| 版本 | 日期 | 修改内容 | 作者 |
|------|------|---------|------|
| v0.1 | 2024-XX-XX | 初稿创建 - 单机Roguelike版 | [姓名] |
| v0.2 | YYYY-MM-DD | [修改说明] | [姓名] |

---

## 10. 技术框架

### 10.1 技术选型

#### 10.1.1 核心框架
| 层级 | 技术/框架 | 版本 | 说明 |
|------|----------|------|------|
| 编程语言 | Python | 3.11+ | 主开发语言 |
| 游戏引擎 | Pygame | 2.5+ | 2D游戏渲染和输入处理 |
| 实体组件 | Esper | 3.0+ | ECS架构实现 |
| 数据存储 | SQLite | 3.0+ | 本地存档数据库 |
| 配置解析 | PyYAML | 6.0+ | 游戏配置和数据表 |
| 随机生成 | NumPy | 1.24+ | 地图生成算法支持 |

#### 10.1.2 可选增强库
| 用途 | 库名 | 说明 |
|------|------|------|
| 性能优化 | Cython | 关键算法编译加速 |
| 音效处理 | Pygame.mixer | 内置音频支持 |
| 高级音效 | PyOpenAL | 3D音效支持 |
| 日志系统 | Loguru | 结构化日志记录 |
| 类型检查 | MyPy | 静态类型检查 |
| 单元测试 | Pytest | 自动化测试框架 |

### 10.2 项目结构

```
├── main.py                 # 游戏入口
├── requirements.txt        # 依赖列表
├── config/                 # 配置文件
│   ├── game_config.yaml    # 游戏核心配置
│   ├── relics/             # 遗物配置
│   ├── enemies/            # 敌人配置
│   ├── characters/         # 角色配置
│   └── levels/             # 关卡配置
├── src/                    # 源代码
│   ├── core/               # 核心系统
│   │   ├── __init__.py
│   │   ├── game.py         # 游戏主循环
│   │   ├── event_manager.py # 事件系统
│   │   ├── state_machine.py # 状态机
│   │   └── resource_manager.py # 资源管理
│   ├── ecs/                # ECS架构
│   │   ├── __init__.py
│   │   ├── components.py   # 组件定义
│   │   ├── processors.py   # 处理器/系统
│   │   └── entities.py     # 实体工厂
│   ├── systems/            # 游戏系统
│   │   ├── __init__.py
│   │   ├── combat_system.py    # 战斗系统
│   │   ├── movement_system.py  # 移动系统
│   │   ├── render_system.py    # 渲染系统
│   │   ├── input_system.py     # 输入系统
│   │   ├── ui_system.py        # UI系统
│   │   └── audio_system.py     # 音频系统
│   ├── generation/         # 生成系统
│   │   ├── __init__.py
│   │   ├── map_generator.py    # 地图生成
│   │   ├── dungeon_generator.py # 地牢生成
│   │   ├── loot_generator.py   # 战利品生成
│   │   └── seed_manager.py     # 种子管理
│   ├── data/               # 数据管理
│   │   ├── __init__.py
│   │   ├── save_manager.py     # 存档管理
│   │   ├── config_loader.py    # 配置加载
│   │   └── database.py         # 数据库操作
│   ├── ui/                 # UI模块
│   │   ├── __init__.py
│   │   ├── screens/        # 界面屏幕
│   │   ├── widgets/        # UI组件
│   │   └── hud.py          # 游戏HUD
│   └── utils/              # 工具函数
│       ├── __init__.py
│       ├── constants.py    # 常量定义
│       ├── helpers.py      # 辅助函数
│       └── logger.py       # 日志配置
├── assets/                 # 游戏资源
│   ├── images/             # 图片资源
│   │   ├── characters/
│   │   ├── enemies/
│   │   ├── items/
│   │   ├── ui/
│   │   └── effects/
│   ├── audio/              # 音频资源
│   │   ├── bgm/            # 背景音乐
│   │   └── sfx/            # 音效
│   ├── fonts/              # 字体文件
│   └── data/               # 数据文件
├── tests/                  # 测试代码
│   ├── test_combat.py
│   ├── test_generation.py
│   └── test_save.py
├── saves/                  # 存档目录
└── docs/                   # 文档
```

### 10.3 核心架构设计

#### 10.3.1 ECS架构说明
```python
# 组件定义示例 (src/ecs/components.py)
@dataclass
class Position:
    x: float
    y: float

@dataclass
class Velocity:
    vx: float
    vy: float

@dataclass
class Health:
    current: int
    maximum: int

@dataclass
class Combat:
    attack: int
    defense: int
    crit_rate: float

@dataclass
class Renderable:
    sprite: pygame.Surface
    layer: int
```

#### 10.3.2 系统处理器示例
```python
# 移动系统 (src/systems/movement_system.py)
class MovementProcessor(esper.Processor):
    def process(self, dt: float):
        for ent, (pos, vel) in self.world.get_components(Position, Velocity):
            pos.x += vel.vx * dt
            pos.y += vel.vy * dt

# 战斗系统 (src/systems/combat_system.py)
class CombatProcessor(esper.Processor):
    def process(self, dt: float):
        for ent, (combat, health) in self.world.get_components(Combat, Health):
            # 处理战斗逻辑
            pass
```

#### 10.3.3 游戏状态机
```python
# 状态机实现 (src/core/state_machine.py)
class GameState(Enum):
    MAIN_MENU = auto()
    CHARACTER_SELECT = auto()
    MAP_NAVIGATION = auto()
    COMBAT = auto()
    SHOP = auto()
    EVENT = auto()
    PAUSE = auto()
    GAME_OVER = auto()
    VICTORY = auto()

class StateMachine:
    def __init__(self):
        self.states: Dict[GameState, State] = {}
        self.current_state: Optional[State] = None
    
    def change_state(self, new_state: GameState, **kwargs):
        if self.current_state:
            self.current_state.exit()
        self.current_state = self.states[new_state]
        self.current_state.enter(**kwargs)
```

### 10.4 关键系统设计

#### 10.4.1 地图生成系统
```python
# 地图生成核心 (src/generation/map_generator.py)
class MapGenerator:
    def __init__(self, seed: int):
        self.rng = np.random.RandomState(seed)
    
    def generate_floor(self, floor_number: int) -> Floor:
        """生成单层地图"""
        # 1. 生成节点布局
        nodes = self._generate_node_layout(floor_number)
        # 2. 分配房间类型
        rooms = self._assign_room_types(nodes)
        # 3. 生成房间内容
        for room in rooms:
            self._populate_room(room, floor_number)
        return Floor(rooms)
    
    def _generate_node_layout(self, floor: int) -> List[Node]:
        """基于图论生成节点连接"""
        # 确保有从入口到BOSS的有效路径
        # 添加分支路径和可选区域
        pass
```

#### 10.4.2 存档系统
```python
# 存档管理 (src/data/save_manager.py)
class SaveManager:
    def __init__(self, db_path: str = "saves/game.db"):
        self.db = sqlite3.connect(db_path)
        self._init_tables()
    
    def save_meta_progress(self, progress: MetaProgress):
        """保存元进度"""
        data = pickle.dumps(progress)
        self.db.execute(
            "INSERT OR REPLACE INTO meta_progress (id, data) VALUES (1, ?)",
            (data,)
        )
        self.db.commit()
    
    def save_run_state(self, run_state: RunState, slot: int = 0):
        """保存当前局进度"""
        # 仅在特定条件下允许保存（如休息点）
        pass
    
    def load_meta_progress(self) -> MetaProgress:
        """加载元进度"""
        pass
```

#### 10.4.3 遗物系统
```python
# 遗物管理 (src/systems/relic_system.py)
@dataclass
class RelicEffect:
    trigger: str  # 'on_hit', 'on_kill', 'on_damaged'等
    effect_type: str
    value: float
    condition: Optional[str] = None

class Relic:
    def __init__(self, relic_id: str, config: dict):
        self.id = relic_id
        self.name = config['name']
        self.rarity = config['rarity']
        self.effects = [RelicEffect(**e) for e in config['effects']]
    
    def apply(self, event: str, context: dict) -> dict:
        """应用遗物效果，返回修改后的上下文"""
        for effect in self.effects:
            if effect.trigger == event:
                context = self._apply_effect(effect, context)
        return context

class RelicManager:
    def __init__(self):
        self.active_relics: List[Relic] = []
        self.relic_pool: Dict[str, Relic] = self._load_relics()
    
    def add_relic(self, relic_id: str):
        """添加遗物到当前Build"""
        relic = self.relic_pool[relic_id]
        self.active_relics.append(relic)
        # 触发遗物获得事件
        
    def trigger_event(self, event: str, context: dict) -> dict:
        """触发所有相关遗物效果"""
        for relic in self.active_relics:
            context = relic.apply(event, context)
        return context
```

### 10.5 配置系统设计

#### 10.5.1 YAML配置示例
```yaml
# config/relics/fire_relics.yaml
relics:
  - id: fire_heart
    name: "火焰之心"
    rarity: epic
    description: "火系伤害+50%"
    effects:
      - trigger: "on_deal_damage"
        effect_type: "damage_bonus"
        value: 0.5
        condition: "element == fire"
    
  - id: burning_touch
    name: "燃烧之触"
    rarity: rare
    description: "攻击附加燃烧效果"
    effects:
      - trigger: "on_hit"
        effect_type: "apply_buff"
        value: "burning"

# config/characters/knight.yaml
character:
  id: knight
  name: "骑士"
  base_stats:
    health: 80
    attack: 12
    defense: 8
    speed: 5
  starting_relic: "rusted_shield"
  special_mechanic: "block_system"
```

#### 10.5.2 配置加载器
```python
# 配置加载 (src/data/config_loader.py)
class ConfigLoader:
    def __init__(self, config_path: str = "config"):
        self.config_path = Path(config_path)
        self._cache: Dict[str, Any] = {}
    
    def load_relics(self) -> Dict[str, dict]:
        """加载所有遗物配置"""
        if 'relics' not in self._cache:
            relics = {}
            for file in (self.config_path / "relics").glob("*.yaml"):
                with open(file, 'r', encoding='utf-8') as f:
                    data = yaml.safe_load(f)
                    for relic in data.get('relics', []):
                        relics[relic['id']] = relic
            self._cache['relics'] = relics
        return self._cache['relics']
    
    def load_character(self, char_id: str) -> dict:
        """加载角色配置"""
        file_path = self.config_path / "characters" / f"{char_id}.yaml"
        with open(file_path, 'r', encoding='utf-8') as f:
            return yaml.safe_load(f)
```

### 10.6 开发规范

#### 10.6.1 代码规范
- 遵循 PEP 8 代码风格
- 使用类型注解（Type Hints）
- 函数和类添加文档字符串（Docstrings）
- 常量使用 UPPER_SNAKE_CASE
- 类名使用 PascalCase
- 函数和变量使用 snake_case

#### 10.6.2 命名规范
```python
# 常量
SCREEN_WIDTH = 1280
SCREEN_HEIGHT = 720
FPS = 60

# 类名
class CombatSystem:
    pass

class MapGenerator:
    pass

# 函数和变量
def calculate_damage(attacker, defender) -> int:
    base_damage = attacker.attack
    defense = defender.defense
    return max(1, base_damage - defense)
```

#### 10.6.3 项目依赖 (requirements.txt)
```
# 核心依赖
pygame>=2.5.0
esper>=3.0
PyYAML>=6.0
numpy>=1.24.0

# 开发依赖
pytest>=7.0
pytest-cov>=4.0
mypy>=1.0
black>=23.0

# 可选依赖
loguru>=0.7.0
Cython>=3.0
```

### 10.7 构建与发布

#### 10.7.1 打包配置
```python
# setup.py
from setuptools import setup, find_packages

setup(
    name="roguelike-game",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "pygame>=2.5.0",
        "esper>=3.0",
        "PyYAML>=6.0",
        "numpy>=1.24.0",
    ],
    entry_points={
        "console_scripts": [
            "roguelike=main:main",
        ],
    },
)
```

#### 10.7.2 使用PyInstaller打包
```bash
# 打包为可执行文件
pyinstaller --onefile --windowed \
    --add-data "config;config" \
    --add-data "assets;assets" \
    --icon=assets/icon.ico \
    main.py
```

---

## 附录

### A. 术语表
| 术语 | 解释 |
|------|------|
| Roguelike | 随机地图、永久死亡、回合制/即时制的游戏类型 |
| Roguelite | 带有元进度系统的Roguelike变体 |
| Build | 角色构筑，指遗物、技能等的组合 |
| Meta Progress | 元进度，跨局保留的解锁内容 |
| Seed | 随机种子，决定地图生成 |
| Permadeath | 永久死亡，角色死亡后本局结束 |
| ProcGen | 程序化生成，随机生成内容 |
| DPS | Damage Per Second，每秒伤害 |
| HP | Health Point，生命值 |
| CD | Cool Down，冷却时间 |
| AoE | Area of Effect，范围效果 |
| CC | Crowd Control，控制技能 |

### B. 参考文档
- [Roguelike游戏设计指南]
- [以撒的结合设计分析]
- [杀戮尖塔设计分析]
- [Hades设计分析]

### C. 联系方式
| 角色 | 姓名 | 联系方式 |
|------|------|---------|
| 策划负责人 | | |
| 美术负责人 | | |
| 程序负责人 | | |
| 项目经理 | | |
